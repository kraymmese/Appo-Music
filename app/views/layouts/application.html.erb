<!DOCTYPE html>
<html>
  <head>
    <title>Kray Music</title>
     <meta name="title" property="og:title" content="Kray Music" />
     <meta name="image" property='og:image' content="<%= image_url('banner.png') %>"/>
     <meta name="description" property="og:description"
       content="Kray Music is a full-stack music app based of Apple Music online streaming platform, with an aim to re-create it's core features with a better recomendation engine, seamless design, and excellent user experience."
     />
     <meta property="og:url" content="https://kray-music.herokuapp.com/"/>
     <meta name="author" content="Thabang Mmese"/>

    <link rel="icon" 
      type="image/png" 
      href="<%= image_url('favicon.png') %>">    
     

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all' %>
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Open+Sans:ital,wght@0,300;0,400;0,600;0,700;0,800;1,300;1,400;1,600;1,700;1,800&display=swap" rel="stylesheet">  
    <%= javascript_include_tag 'application' %>
    <% if current_user %>
      <script>
        window.currentUser = {
          "id": <%= current_user.id %>,
          "username": "<%= current_user.username %>",
          "settings": {
            loop: <%= current_user.settings.loop %>, 
            shuffle: <%= current_user.settings.shuffle %>}
        }
      </script>
    <% end %>
      <!-- <script>
         window.logo = "<#%= image_url('logo.png') %>"
      </script> -->
  </head>

  <body>
    <%= yield %>
  </body>
</html>
